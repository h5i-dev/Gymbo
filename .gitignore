/target
# The EventSpy jar is built by java/jv-profiler/build.sh, not committed.
java/jv-profiler/target/
**/*.rs.bk
*.pdb

# cargo-dist output
/dist

# Reference clones (spec sources, not vendored)
/_reference

# Local scratch
/.jv
/scratch
*.log
