[package]
name = "jv-model-builder"
description = "Builds Maven effective POMs: inheritance, interpolation, profiles, BOM imports"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
jv-model = { workspace = true }
jv-version = { workspace = true }
# Only for the vendored plexus lifecycle bindings; POMs are parsed by jv-model.
dirs = { workspace = true }
quick-xml = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
